# When a task is completed
- Format changed Dart files with `dart format`.
- Run targeted `flutter analyze` for the touched feature/files first.
- If relevant, run focused tests before broader validation.
- Summarize changed files and note a sensible next feature to refactor.